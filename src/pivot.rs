pub struct Pivot {
    crs: String,
}

pub struct SphincsBlock {
    id: u64,
    cluster: u64,
    nonce: u64,
    sphincs_pk: String,
}

impl Pivot {
    pub fn new() -> Self {

    }
    pub fn connect(){

    }
}

