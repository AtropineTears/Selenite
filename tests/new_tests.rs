#[cfg(test)]
mod tests {
    use selenite::certificate::SeleniteCertificate;
    use super::*;
    use selenite::certificates::*;
    #[test]
    fn new_cert(){
        let cert = SeleniteCertificate::new("
        
        ");
    }
}